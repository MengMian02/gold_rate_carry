# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Data: raw files are NOT committed — both must be obtained locally before running.
# GLD auto-fetches via yfinance on first run; DFII10 must be manually downloaded from FRED
# and placed at data/raw/DFII10.csv (see loaders.py's error message). merged.csv is
# generated by clean.py. (data/decisions.md and data/source_note.md remain tracked.)
data/raw/gld_raw.csv
data/raw/DFII10.csv
data/merged.csv

# Results: all generated on every run
result/*
!result/.gitkeep
!result/report.html

# Local working notes

# Local Claude Code project instructions (not shared)
/CLAUDE.md

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
