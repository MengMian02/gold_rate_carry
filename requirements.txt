# Core data + numerics
pandas
numpy

# Data sources
yfinance            # GLD prices (DFII10 is a manual FRED CSV download, no package needed)

# Evaluation / stats
scipy               # block bootstrap, significance testing
quantstats          # performance metrics (Sharpe, drawdown, CAGR, etc.)

# Charts + report rendering
matplotlib
jinja2              # HTML report templating

# Notebook
jupyter
